/**
 * Returns navigation action based on deep link and based on auth state
 */
const handleDeepLink = (link: string, isFromRoot: boolean) => {
  console.log('devug handleDeepLink', link, 'isFromRoot', isFromRoot)
}

export default handleDeepLink
